@model IEnumerable<SIIR.Models.ViewModels.DocumentVM>

@* <div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-8">
            <form method="post" enctype="multipart/form-data" asp-action="GuardarDocumentos">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1 class="display-4 mb-0">Documentos</h1>
                    <button type="submit" class="btn btn-primary">
                        <i class="fa-solid fa-save me-2"></i>Guardar
                    </button>
                </div>

                <br/>
                
                @foreach (var document in Model)
                {
                    <div class="mb-4">
                        
                        <label class="form-label fs-5 fw-bold">@document.Name</label>
                        <p class="text-muted mb-2" style="font-size: 0.9rem;">@document.Description</p>
                        
                        <div class="border rounded p-2">
                            <div class="d-flex align-items-center">
                                <div class="position-relative" style="flex: 1;">
                                    <input type="file" class="form-control" id="file_@document.Id" name="files" />
                                </div>
                        
                                <div class="d-flex align-items-center ms-3">
                                    <a asp-action="ViewDocument" asp-controller="Document" asp-route-id="@document.Id"
                                       class="btn btn-outline-primary btn-sm me-2" title="Visualizar">
                                        <i class="fa-solid fa-eye"></i>
                                    </a>
                                    <a asp-action="DownloadDocument" asp-controller="Document" asp-route-id="@document.Id"
                                       class="btn btn-outline-success btn-sm me-2" title="Descargar">
                                        <i class="fa-solid fa-download"></i>
                                    </a>
                                    <a asp-action="DeleteDocument" asp-controller="Document" asp-route-id="@document.Id"
                                       class="btn btn-outline-danger btn-sm" title="Eliminar">
                                        <i class="fa-solid fa-trash"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </form>
        </div>
    </div>
</div> *@
<style>
    .form-control[type="file"] {
        padding: 0.375rem 0.75rem;
    }

        .form-control[type="file"]::file-selector-button {
            padding: 0.375rem 0.75rem;
            margin: -0.375rem 0.75rem -0.375rem -0.75rem;
            background-color: #e9ecef;
            border: 0;
            border-right: 1px solid #ced4da;
        }

    .btn-sm {
        min-width: 32px;
        height: 32px;
        padding: 0.25rem 0.5rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .btn-primary {
        background-color: #1a3c6e;
        border-color: #1a3c6e;
    }
</style>